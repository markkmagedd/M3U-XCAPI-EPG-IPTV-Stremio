{
  "version": 2,
  "builds": [
    {
      "src": "serverless.js",
      "use": "@vercel/node",
      "config": { "maxLambdaSize": "50mb", "includeFiles": "src/**" }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/serverless.js" }]
}
